import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: baseline-browser-mapping warnings are informational
  // The package is up to date (v2.8.32), but the browser data within it may be older
  // This doesn't affect functionality and can be safely ignored
  // The warning will disappear when the package maintainers update the browser data
};

export default nextConfig;
